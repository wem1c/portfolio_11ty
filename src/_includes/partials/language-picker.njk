{% if locale === "en" %}
    <a href="{{ page.url | locale_url('en', 'cnr') }}"
       class="underline-none">🇲🇪</a>
{% else %}
    <a href="{{ page.url | locale_url('cnr', 'en') }}"
       class="underline-none">🇺🇸</a>
{% endif %}
