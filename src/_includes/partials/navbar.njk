<nav class="cluster" data-justify="start">
    {% for item in navigation.items %}
        <a href="/{{ locale }}{{ item.url }}" hreflang="{{ locale }}" {{ helpers.getLinkActiveState(item.url, page.url) | safe }}>{{ item.name }}</a>
    {% endfor %}
</nav>
